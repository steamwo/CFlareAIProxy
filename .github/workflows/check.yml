name: Check CFlareAIProxy

on:
  pull_request:

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Apply and clean bootstrap
        run: |
          python .github/pr37-fix-hotfix.py
          python .github/pr37-fix.py
          python .github/pr37-fix-post.py
          rm -f \
            .github/pr37-fix.py \
            .github/pr37-fix-hotfix.py \
            .github/pr37-fix-post.py \
            .github/workflows/apply-pr37-review-fixes.yml \
            validation-error.log \
            pr37-fix.log
          git show "${{ github.event.pull_request.base.sha }}:.github/workflows/check.yml" > .github/workflows/check.yml
          git diff --check
      - uses: pnpm/action-setup@v4
        with:
          version: 11.9.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run standard validation
        run: pnpm run check
      - name: Build production web bundle
        run: pnpm run build:web
      - name: Wrangler dry run
        run: pnpm run config:check
      - name: Commit validated fixes
        run: |
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix PR 37 review findings"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
