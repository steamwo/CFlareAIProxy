{
  "name": "cflareapi-proxy-bridge",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Authenticated streaming proxy bridge for CFlareAPI provider-level HTTP/HTTPS/SOCKS egress.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs"
  },
  "dependencies": {
    "proxy-agent": "8.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
