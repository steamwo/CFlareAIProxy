# AGENTS.md

本文件适用于整个仓库。子目录若存在更具体的 `AGENTS.md`，以更具体的规则为准。

## 目标

在不破坏现有部署、数据和协议兼容性的前提下，完成最小、可验证、可回滚的修改。不要把“局部测试通过”或“现有环境能用”等同于“目标提交可全新构建和部署”。

## 修改前

1. 先阅读相关代码、测试、配置和文档，确认真实调用链与入口。
2. 检查目标分支最新状态；PR 必须基于最新目标分支，避免只验证旧基线。
3. 明确变更范围、兼容性影响、数据影响和回滚方式。
4. 不得顺手重构无关代码，不得覆盖用户或其他分支的未合并改动。

## 项目结构与不变量

- `src/worker.ts` 是 Wrangler 入口；`src/index.ts` 承载主要 HTTP、Queue 和业务路由。
- `wrangler.jsonc` 中声明的 Durable Object 类必须由配置的入口文件具名导出。
- `AccountPool` 和 `RateLimiter` 的类名、绑定名、迁移记录不得随意修改。
- `/v1/*`、`/admin/api/*`、`/oauth/*`、Queue consumer、Cron handler 和静态资源路由必须继续可用。
- 内置授权账号与 OpenAI-compatible provider API Key 的管理职责不得混淆。
- 账号池和概览统计必须使用有界查询；禁止恢复随日志历史无限增长的全表聚合。
- 基础调用统计与请求明细日志开关保持解耦。
- 上游协议转换、错误分类、流式响应和 usage 记录修改必须保持现有公开 API 兼容。

## Cloudflare 配置

修改以下任一内容时，必须同时检查相关绑定、入口导出、类型和部署行为：

- `wrangler.jsonc`
- Worker 入口、Cron、Queue、Durable Objects
- D1、KV、Secrets、Static Assets
- 部署或资源初始化脚本

要求：

- 不提交真实 Secret、Token、密码、数据库 ID 或 KV ID。
- 不绕过 `scripts/deploy.mjs` 的资源和 Secret 初始化流程。
- 修改 Worker 入口时，核对默认导出以及所有 Wrangler 引用的具名导出。
- Cloudflare 配置变更必须通过 Wrangler dry-run；仅 Web 构建成功不算部署验证。

## 数据库迁移

- 迁移文件只追加，不修改已发布迁移的语义或顺序。
- 使用连续、唯一、可排序的文件名；重命名后全仓搜索旧名称并更新脚本和文档。
- 新增列、表或索引时，考虑旧数据库升级、重复执行和查询成本。
- 删除或改写数据必须明确保留范围、时间边界和失败行为。
- 涉及迁移时至少验证本地迁移；发布前确认远程迁移路径。

## 安全与隐私

- 凭据必须保持加密存储；不得在日志、错误、测试快照或 API 响应中泄露明文。
- 对外错误应可诊断但不泄露内部配置、密钥、SQL 或上游敏感响应。
- 新增管理接口必须复用现有认证和授权机制。
- 代理、OAuth、回调 URL、用户输入和上游响应必须按不可信数据处理。
- 不降低 CORS、Cookie、缓存或安全 Header 的现有保护，除非需求明确且有验证。

## TypeScript、后端与测试

- 保持严格 TypeScript；不要用无依据的 `any`、非空断言或类型强转掩盖问题。
- 优先复用现有模块、错误类型、解析函数和 provider 抽象。
- 修复缺陷必须补充能复现根因的回归测试。
- 测试应验证行为和边界，不依赖真实网络、真实账号或不稳定时间。
- 修改路由、provider、配额、流式响应、并发或存储逻辑时，检查关联测试和失败路径。

## Web 管理台

- 保持 Vue 3、Pinia、Vue Router 和 Naive UI 的现有组织方式。
- API 返回结构变化必须同步类型、解析、空状态、错误状态和回归测试。
- 页面必须兼顾加载、空数据、部分数据、分页和移动端布局。
- 不直接依赖未经归一化的后端数据；边界处统一处理兼容格式。
- 不破坏旧静态资源失效后的刷新恢复和缓存策略。

## 文档与配置同步

行为、环境变量、部署步骤、API 或架构发生变化时，更新相关文档和示例配置。文档必须描述当前真实行为，不复制已经失效的命令、路径或文件名。

## 验证要求

安装依赖：

```bash
pnpm install --frozen-lockfile
```

常规代码变更至少执行：

```bash
pnpm run check
```

涉及 Web 构建、部署、Cloudflare 配置、迁移或 Worker 入口时，还必须执行：

```bash
pnpm run build:web
pnpm run config:check
```

按变更范围补充：

```bash
pnpm run db:migrate:local
pnpm run smoke
pnpm run smoke:admin
```

不得因为某个步骤配置了 `continue-on-error` 就认为 CI 通过；以工作流最终结论和失败日志为准。

## Git 与 PR

- 从最新 `dev` 创建功能分支，分支名使用 `agent/<description>`。
- 每个 PR 聚焦一个完整目的，提交信息和 PR 描述说明根因、影响、验证与回滚。
- 不直接强制更新 `main` 或 `dev`，不改写共享分支历史。
- 默认流程：功能分支 → `dev` → 验证部署 → `main`。
- 存在依赖的多个 PR 合并后，必须验证最终组合提交，不能引用任一旧分支的绿色构建。
- CI 未通过不得合并；如用户明确要求接受风险，仍须准确说明失败项，不得声称可部署。
- `dev → main` 前确认 `dev` 不落后、目标提交 CI 绿色、Wrangler dry-run 成功，并保留明确回滚点。

## 完成标准

任务完成时应同时满足：

- 修改范围与需求一致，无无关变更。
- 相关测试、类型检查、构建和部署检查通过。
- 已检查安全、兼容性、迁移和回滚风险。
- 代码、配置、测试和文档保持一致。
- 最终报告列出变更、验证结果、PR/提交和仍存在的已知限制。
