<!-- Internal scope note for Issue #72. Remove before merge if route-level override lands in this PR. -->
Provider/model prompt-cache opt-in is implemented in this branch. Route-level override remains a separate Issue #72 follow-up because the provider adapter does not currently receive route options.
